# AI Specs Index

All specs for this package, in dependency order.

---

## Design Foundation

| # | Title | Status | Spec File |
|---|-------|--------|-----------|
| 000 | Mod Design | `Draft` | *(not yet authored — design rationale lives in prior conversation history rather than a written spec)* |

---

## Implementation Specs

| # | Title | Status | Spec File | Output Files |
|---|-------|--------|-----------|--------------|
| 001 | Presence-Gated Chunk Pregeneration Scheduler | `Implemented` | `ai-specs/specs/001-chunk-pregen-scheduler.md` | `ChunkyFriends.java`, `config/ChunkyFriendsConfig.java`, `chunky/ChunkyGateway.java`, `player/PlayerPregenState.java`, `player/PlayerStateStore.java`, `scheduler/RingCurve.java`, `scheduler/PlayerSelector.java`, `scheduler/PregenScheduler.java` (renamed 2026-08-03 from the `ChunkyShared*`/`com.othstudios.chunkyshared` names the spec text below still refers to — see Agent Log) |
| 002 | Admin Commands, Status Visibility & Map Preview | `Implemented` | `ai-specs/specs/002-admin-commands-status-visibility.md` | `scheduler/ActiveJobSnapshot.java`, `scheduler/TerrainPreviewSampler.java`, `scheduler/ChunkGridLayout.java`, `network/MapPreviewRequestPayload.java`, `network/MapPreviewResponsePayload.java`, `network/StatusRequestPayload.java`, `network/StatusResponsePayload.java`, `network/PlayersRequestPayload.java`, `network/PlayersResponsePayload.java` (new); modifies `scheduler/PregenScheduler.java`, `scheduler/PlayerSelector.java`, `command/ChunkyFriendsCommand.java`, `ChunkyFriends.java`, `network/ConfigNetworking.java`, `client/screen/ChunkyFriendsConfigScreen.java`, `client/network/ConfigNetworkingClient.java`, `lang/en_us.json` — the map preview centers on the requesting player's real position (not spawn/synthetic players) and streams in nearest-first batches; see the spec's Post-Implementation Notes for the full revision history |
| 003 | Full Configuration Parity (GUI & CLI) | `Draft` | `ai-specs/specs/003-status-visibility-and-full-config-parity.md` | `network/ConfigValues.java`, `network/ConfigField.java`, `network/RangeViolation.java`, `client/screen/ChunkyFriendsAdvancedConfigScreen.java` (new); modifies `client/screen/ChunkyFriendsConfigScreen.java`, `network/ConfigNetworking.java`, `network/ConfigStatePayload.java`, `network/ConfigUpdatePayload.java`, `network/ConfigRequestPayload.java`, `config/ChunkyFriendsConfig.java`, `command/ChunkyFriendsCommand.java`, `lang/en_us.json` — exposes the 4 currently-JSON-only config fields via GUI/CLI, bumps the config network protocol to `_v2`, and adds exact-bounds rejection messages. Filename still says "status-visibility" from its original scope; kept unrenamed since it's an established reference (see Agent Log 2026-08-07) |

---

## Future Specs (Not Yet Written)

| # | Title | Status | Depends On | Notes |
|---|-------|--------|------------|-------|
| 004 | Client Configuration GUI & Command | `Implemented (no spec)` | 001 | Ring count / max radius / linear-quadratic curve, editable from a client GUI or server-side (`/chunkyfriends config ...`, RCON-safe) — `/chunkyfriends gui` (also server-side; see its own `@implNote` for why) sends the connected player's client an instruction to open the screen. Gated by an op-level permission (`chunky-friends:config`), plus client-side detection of a missing/version-mismatched server mod. Built directly per user request, skipping the spec-first step this project otherwise follows — a spec should be retroactively written here (see Agent Log) so this has the same documentation trail as everything else |
| 005 | Retroactive spec for Client Configuration GUI & Command | `Draft` | 004 | Documents the already-implemented row 004 above with the same rigor (data contract, acceptance criteria, test requirements) as the numbered specs — referenced as an outstanding follow-up in both 002's and 003's Post-Implementation Notes |
| 006 | HUD / Sodium Settings UI | `Draft` | 001 | Only if wanted — 001 is a headless background service with no client-facing UI. Renumbered from 003 on 2026-08-07 to free that number for the real, on-disk Spec 003 (Full Configuration Parity) — see Agent Log |

---

## Dependency Graph

```
001 (Presence-Gated Chunk Pregeneration Scheduler) ← foundation; everything else builds on this
  └─ 002 (Admin Commands, Status Visibility & Map Preview)
  └─ 004 (Client Configuration GUI & Command, implemented without a spec)
       └─ 003 (Full Configuration Parity — GUI & CLI)
```

---

## Agent Log

| Date | Event | Agent | Notes |
|------|-------|-------|-------|
| 2026-08-02 | Spec 001 authored | Claude (Anthropic) via Cowork | Covers the ring-tier scheduler, min-tier-first selection with tie-break, presence-gated pause/resume, and `GenerationProgressEvent.complete()`-based completion detection worked out in design discussion |
| 2026-08-02 | Spec 001 implemented | Claude (Sonnet 5) via Claude Code | All Create-listed files added; `./gradlew build` and unit tests pass. Manual (live-server) acceptance criteria not exercised — no running dedicated server with Chunky available. See spec's Post-Implementation Notes for deviations |
| 2026-08-02 | "004" client config GUI/command built without a spec | Claude (Sonnet 5) via Claude Code | User requested a client-side config GUI plus CLI parity plus client/server version-mismatch handling directly in chat; user explicitly chose "implement directly" over "draft a spec first" when asked. Added `network/ConfigRequestPayload`, `network/ConfigStatePayload`, `network/ConfigUpdatePayload`, `network/ConfigNetworking`, `command/ChunkySharedCommand`, `client/network/ConfigNetworkingClient`, `client/screen/ChunkySharedConfigScreen`; modified `ChunkyShared`, `ChunkySharedClient`, `ChunkySharedConfig`, `en_us.json`. Builds and tests pass; not manually verified against a live client+server pair. Someone should retroactively author a real 004 spec from this row so it gets the same rigor (data contract, acceptance criteria, test requirements) as 001 |
| 2026-08-03 | Project renamed: com.othstudios.chunkyshared → com.onthehill.chunkyfriends | Claude (Sonnet 5) via Claude Code | User-requested rename, not a spec-driven change. Java package (`com.othstudios.chunkyshared` → `com.onthehill.chunkyfriends`), Maven group, mod ID (`chunky-shared` → `chunky-friends`, kept hyphenated per the user's earlier explicit choice to deviate from `fabric-mod-standards.md`'s snake_case rule), display name ("Chunky Shared" → "Chunky Friends"), every `ChunkyShared*` class (→ `ChunkyFriends*`), the asset folder, mixin config filenames, lang keys, the persisted config filename (`chunkyshared.json` → `chunkyfriends.json`), and the project's own root directory were all updated in one pass via ordered bulk text substitution + `git mv`, then verified with a full `./gradlew build`. Network payload channel IDs kept their existing `_v1` suffixes unchanged (that versions the payload *schema*, unrelated to the project's name). The GitHub repo itself (`OnTheHill-Studios/com.othstudios.chunkyshared`) was **not** renamed by the agent — that's an externally-visible action left for the user to trigger via `gh repo rename` or the GitHub web UI. **Deliberately not touched**: the historical prose inside `ai-specs/specs/001-chunk-pregen-scheduler.md` still refers to the old `ChunkyShared`/`chunky-shared` names throughout, since it's a record of what was true when that spec was implemented — only this index's Output Files pointers were updated to the current filenames |
| 2026-08-03 | Spec 002 authored | Claude (Sonnet 5) via Cowork | Covers `/chunkyfriends status` (single active job's ring tier and progress) and `/chunkyfriends players` (every not-evicted player's ring tier), both reachable from RCON/console/player and from two new client config-screen buttons, plus unconditional server-log output for each. Extracts `PlayerSelector`'s qualifying-window check into a reusable `isEligible` method and adds a new `ActiveJobSnapshot` type. Flags a pre-existing edge case in `PlayerPregenState.lastSeenEpochMillis` (defaults to `0` for a never-disconnected player, which reads as "evicted" under the existing window check) as a documented-but-out-of-scope follow-up rather than silently fixing it here |
| 2026-08-07 | Spec 002 revised — map preview folded in | Claude (Sonnet 5) via Cowork | User asked whether the config screen could show a real Explorer's-Map-style background of the world's spawn area with ring circles overlaid, without uploading sample images. Added a server-sampled (already-generated-chunks-only, no forced generation) terrain color grid sent to the client over a new `MapPreviewRequestPayload`/`MapPreviewResponsePayload` pair, rendered as the screen's background with three synthetic (not real-player) example ring sets on top. User specified: fold into 002 rather than a new spec; example players are synthetic, spaced as a fraction of the currently-entered `maxRadiusChunks` (landing at "a few hundred chunks apart" near a 1000-chunk radius); preview should stay useful from a realistic default (~100 chunks) up through 1000 chunks, degrading gracefully (clamped, with a note) beyond that. New pure/testable `ExamplePlayerLayout` utility added for the spacing math |
| 2026-08-07 | Spec 002 revised again — accurate chunk-boundary grid overlay added | Claude (Sonnet 5) via Cowork | User asked for a thin grid over the map preview showing real chunk boundaries. Added a client-side-only, pure-math `ChunkGridLayout` utility computing grid line pixel positions from the existing response's `originBlockX`/`originBlockZ`/`blocksPerPixel` (no new network data needed) — lines are aligned to true world chunk edges (spawn is not generally chunk-aligned, so this is not simply "every 16 pixels from center"), and the grid step subsamples to a coarser real-chunk multiple at large preview radii rather than aliasing into a solid wash once chunks go sub-pixel. Rendered beneath the ring circles in its own color so it reads as a reference layer, not a fourth data series |
| 2026-08-07 | Spec 002 revised a third time — always-visible scale legend added | Claude (Sonnet 5) via Cowork | User asked for a key/legend giving a pixel-to-chunk sense of scale, specifically to compensate for the grid subsampling at large preview radii noted in the prior revision. Rather than an independent scale bar with its own round-number logic, the legend directly reports the same `gridStepChunks` value the grid overlay already computes each frame (in both chunk and block units), so the legend and the rendered grid can never drift out of sync — reads as "Grid: N chunks (M blocks)", reusing the existing loading-state message before the first preview response arrives |
| 2026-08-07 | Spec 002 and Spec 003 separated — duplicate content removed | Claude (Sonnet 5) via Cowork | User asked whether Spec 2 and Spec 3 were cleanly separated after noticing two files numbered "002" on disk and heavy overlap between the on-disk `002-admin-commands-status-visibility.md` and `003-status-visibility-and-full-config-parity.md`. Root cause: `003` was authored on 2026-08-03 against an earlier renumbering generation where "002" meant a different, since-superseded draft; that superseded draft's admin-visibility content (status/players commands, `ActiveJobSnapshot`, the `isEligible` extraction) was later restored under today's live Spec 002, but `003` was never trimmed to match, leaving its "Part B" duplicating what Spec 002 now already covers. Fix: rewrote `003-status-visibility-and-full-config-parity.md` down to config-parity-only content (its former "Part A"); removed all `PregenScheduler`/`PlayerSelector`/`ActiveJobSnapshot`/status-players material, adjusted its GUI section to add only the "Advanced..." button (Status/Players already added by Spec 002), and updated its Depends On/Supersedes/Coordination notes accordingly. Also found a third, unrelated file sharing the "002" number — `002-config-screen-layout-and-ring-preview.md` (`Draft`, not referenced anywhere in this index) — describing an older, schematic hypothetical-player ring preview on a fogged map texture; superseded in substance by Spec 002's now-implemented real-terrain map preview, so moved to `ai-specs/specs/_to_delete/`. Added Spec 003 to the Implementation Specs table above, and renumbered the Future Specs placeholder for "HUD / Sodium Settings UI" from 003 → 006 to free the number, adding an explicit row (005) for the still-outstanding retroactive spec for Spec 004's already-implemented work. Did not touch `003`'s filename (still says "status-visibility") since it's an established on-disk reference — only its content and index metadata changed |
| 2026-08-07 | Spec 002 implemented | Claude (Sonnet 5) via Claude Code | All Create-listed files added and all Modify-listed files updated per the spec, including the map preview. `./gradlew build` (compile, unit tests, jar) succeeds. No local decompiled Minecraft sources were available, so every non-obvious API this spec touches (`ServerLevel` access, non-generating chunk existence checks, `MapColor` packing, `NativeImage`/`DynamicTexture`/texture registration, the client's chat-command-send path, and — biggest surprise — the GUI rendering API, which is not the traditional `Screen.render(GuiGraphics,...)` pair in this installed Minecraft version but a restructured `Screen.extractRenderState(GuiGraphicsExtractor,...)`/`Renderable` model) was verified directly against the real compiled jars via `javap` rather than assumed from memory. **No running Minecraft client/server was available to the agent in this environment**, so the initial pass was build-verified only — the user then live-tested it directly across 7 follow-up rounds (see the spec's Post-Implementation Notes for the full log), surfacing and fixing real issues an offline build can't catch: raw custom-drawn text never actually rendering (moved to real widgets), a fixed-pixel layout that didn't respect window size/aspect ratio (rebuilt as three responsive columns), terrain not rendering (root-caused through two separate bugs — a vanilla chunk-read limitation and a missing texture upload call), the preview being centered on world spawn instead of the viewer's own position (the map preview's design was changed to match, replacing three synthetic example players with one real one), a chunk-load safety cap producing an arbitrary-looking scattered render (replaced with distance-sorted, streamed batching), and a stale-response race the streaming change introduced (fixed with a request id). Marked `Implemented` by the user on 2026-08-07 after this iteration; a few of the original Map Preview acceptance criteria describe the now-superseded synthetic-example-player design and are noted as superseded rather than met, per the spec's own Acceptance Criteria section |
